{
  "engines": {
    "node": ">=24 <25"
  },
  "name": "backend-tests",
  "version": "2.0.0",
  "description": "Run unit tests on this project.",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:report": "vitest run -- --coverage --costs",
    "test:watch": "chokidar \"tests/**/*.ts\" \"contracts/**/*.clar\" -c \"npm run test:report\"",
    "stamp:once": "node scripts/stamp-once.mjs",
    "stamp:batch": "node scripts/stamp-batch.mjs",
    "stamp:stop": "node scripts/stamp-stop.mjs"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@stacks/clarinet-sdk": "^3.11.0",
    "@stacks/transactions": "^7.3.0",
    "@stacks/wallet-sdk": "^7.2.0",
    "@types/node": "24.x",
    "chokidar-cli": "^3.0.0",
    "vitest": "^4.0.7",
    "vitest-environment-clarinet": "^3.0.2"
  }
}
